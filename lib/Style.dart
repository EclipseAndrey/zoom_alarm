import 'package:flutter/material.dart';

const Color cMainText = Color.fromRGBO(40, 40, 40, 1);
const Color c282828 = cMainText;
const Color cMainBlack = cMainText;

const Color cTitles = Color.fromRGBO(47, 82, 127, 1);
const Color c2f527f = cTitles;

const Color cWhite = Color.fromRGBO(255, 255, 255, 1);
const Color cffffff = cWhite;

const Color cBG = Color.fromRGBO(250, 250, 250, 1);
const Color cBackground = cBG;
const Color cfafafa = cBG;

const Color cDefault = Color.fromRGBO(209, 211, 215, 1);
const Color cd1d3d7 = cDefault;

const Color cLinks = Color.fromRGBO(88, 148, 188, 1);
const Color c5894bc = cLinks;

const Color cButtons = Color.fromRGBO(141, 205, 224, 1);
const Color c8dcde0 = cButtons;

const Color cSecondaryButtons = Color.fromRGBO(203, 254, 254, 1);
const Color ccbfefe = cSecondaryButtons;

const Color cPay = Color.fromRGBO(255, 0, 0, 1);
const Color cff0000 = cPay;

const Color cIcons = Color.fromRGBO(98, 135, 161, 1);
const Color c6287A1 = cIcons;

const Color cForms = Color.fromRGBO(240, 245, 239, 1);
const Color cf0f5ef = cForms;

const Color cSecondaryForms = Color.fromRGBO(180, 191, 179, 1);
const Color cb4bfb3 = cSecondaryForms;

const Color cPlaceHolder = Color.fromRGBO(122, 139, 163, 1);
const Color c7A8BA3 = cPlaceHolder;

const Color cBlue = Colors.blue;

const List<Color> cBackgroundGradient = [Color(0xFFFFFFFF), cBG];

const List<BoxShadow> shadowContainer = [
  BoxShadow(
    color: Color.fromRGBO(122, 139, 163, 0.3),
    spreadRadius: 0,
    blurRadius: 5,
    offset: Offset(0, 0), // changes position of shadow
  )
];

const String fontFamily = "MPLUS";
